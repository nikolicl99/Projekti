unit Pitanje3;

interface

uses
  System.SysUtils, System.Types, System.UITypes, System.Classes, System.Variants,
  FMX.Types, FMX.Controls, FMX.Forms, FMX.Graphics, FMX.Dialogs, FMX.Memo.Types,
  FMX.StdCtrls, FMX.Controls.Presentation, FMX.ScrollBox, FMX.Memo,
  FireDAC.Stan.Intf, FireDAC.Stan.Option, FireDAC.Stan.Param,
  FireDAC.Stan.Error, FireDAC.DatS, FireDAC.Phys.Intf, FireDAC.DApt.Intf,
  FireDAC.Stan.Async, FireDAC.DApt, Data.DB, FireDAC.Comp.DataSet,
  FireDAC.Comp.Client;

type
  TfrmPitanje3 = class(TForm)
    Memo: TMemo;
    dvesta: TRadioButton;
    petsto: TRadioButton;
    Dalje: TButton;
    Nazad: TButton;
    vise: TRadioButton;
    StyleBook: TStyleBook;
    Forma: TPanel;
    StyleBook1: TStyleBook;
    procedure FormActivate(Sender: TObject);
    procedure FormClose(Sender: TObject; var Action: TCloseAction);
    procedure NazadClick(Sender: TObject);
    procedure DaljeClick(Sender: TObject);
    function ProveraNizaStraneManje(const brojStrana: Integer; const OriginalniNiz: TArray<Integer>): TArray<Integer>;
    function ProveraNizaStraneIzmedju(const brojStranaManje: Integer; const brojStranaVece: Integer; const OriginalniNiz: TArray<Integer>): TArray<Integer>;
    function ProveraNizaStraneVise(const brojStrana: Integer; const OriginalniNiz: TArray<Integer>): TArray<Integer>;
  private
    { Private declarations }
  public
    { Public declarations }
    NizPitanje3: TArray<Integer>;
  end;

var
  frmPitanje3: TfrmPitanje3;

implementation

{$R *.fmx}
uses Main, Pitanje2, Pitanje2Tema, Pitanje2Knjiga, Pitanje4;

procedure TfrmPitanje3.DaljeClick(Sender: TObject);
var
  Poruka: String;
  i: integer;
begin
//  if frmPitanje2.Pitanje2ID = 1 then
//  begin
    if dvesta.IsChecked then
    begin
      frmMain.NizPitanje3 := ProveraNizaStraneManje(200, frmMain.NizKnjige);
//      if Length(frmMain.NizPitanje3) > 0 then
//      begin
//        Poruka := '';
//        for i := Low(frmMain.NizPitanje3) to High(frmMain.NizPitanje3) do
//        begin
//          Poruka := Poruka + IntToStr(frmMain.NizPitanje3[i]) + ', ';
//        end;
//        SetLength(Poruka, Length(Poruka) - 2);
        ShowMessage('Knjige sa manje od 200 strana: ' + IntToStr(Length(frmMain.NizPitanje3)));
//      end;
      frmPitanje4.Show;
      self.Hide;
    end;
    if petsto.IsChecked then
    begin
      frmMain.NizPitanje3 := ProveraNizaStraneIzmedju(200, 500, frmMain.NizKnjige);
//      if Length(frmMain.NizPitanje3) > 0 then
//      begin
//        Poruka := '';
//        for i := Low(frmMain.NizPitanje3) to High(frmMain.NizPitanje3) do
//        begin
//          Poruka := Poruka + IntToStr(frmMain.NizPitanje3[i]) + ', ';
//        end;
//        SetLength(Poruka, Length(Poruka) - 2);
//        ShowMessage('Knjige sa između 200 i 500 strana: ' + Poruka);
//      end;
      frmPitanje4.Show;
      self.Hide;
    end;
    if vise.IsChecked then
    begin
      frmMain.NizPitanje3 := ProveraNizaStraneVise(500, frmMain.NizKnjige);
//      if Length(frmMain.NizPitanje3) > 0 then
//      begin
//        Poruka := '';
//        for i := Low(frmMain.NizPitanje3) to High(frmMain.NizPitanje3) do
//        begin
//          Poruka := Poruka + IntToStr(frmMain.NizPitanje3[i]) + ', ';
//        end;
//        SetLength(Poruka, Length(Poruka) - 2);
//        ShowMessage('Knjige sa više od 500 strana: ' + Poruka);
//      end;
      frmPitanje4.Show;
      self.Hide;
    end;
  end;
//  if frmPitanje2.Pitanje2ID = 2 then
//  begin
//    if dvesta.IsChecked then
//    begin
//      frmMain.NizPitanje3 := ProveraNizaStraneManje(200, frmMain.NizKnjige);
//      if Length(frmMain.NizPitanje3) > 0 then
//      begin
//        Poruka := '';
//        for i := Low(frmMain.NizPitanje3) to High(frmMain.NizPitanje3) do
//        begin
//          Poruka := Poruka + IntToStr(frmMain.NizPitanje3[i]) + ', ';
//        end;
//        SetLength(Poruka, Length(Poruka) - 2);
//        ShowMessage('Knjige sa manje od 200 strana: ' + Poruka);
//      end;
//      frmPitanje4.Show;
//      self.Hide;
//    end;
//    if petsto.IsChecked then
//    begin
//      frmMain.NizPitanje3 := ProveraNizaStraneIzmedju(200, 500, frmMain.NizKnjige);
//      if Length(frmMain.NizPitanje3) > 0 then
//      begin
//        Poruka := '';
//        for i := Low(frmMain.NizPitanje3) to High(frmMain.NizPitanje3) do
//        begin
//          Poruka := Poruka + IntToStr(frmMain.NizPitanje3[i]) + ', ';
//        end;
//        SetLength(Poruka, Length(Poruka) - 2);
//        ShowMessage('Knjige sa između 200 i 500 strana: ' + Poruka);
//      end;
//      frmPitanje4.Show;
//      self.Hide;
//    end;
//    if vise.IsChecked then
//    begin
//      frmMain.NizPitanje3 := ProveraNizaStraneVise(500, frmMain.NizKnjige);
//      if Length(frmMain.NizPitanje3) > 0 then
//      begin
//        Poruka := '';
//        for i := Low(frmMain.NizPitanje3) to High(frmMain.NizPitanje3) do
//        begin
//          Poruka := Poruka + IntToStr(frmMain.NizPitanje3[i]) + ', ';
//        end;
//        SetLength(Poruka, Length(Poruka) - 2);
//        ShowMessage('Knjige sa više od 500 strana: ' + Poruka);
//      end;
//      frmPitanje4.Show;
//      self.Hide;
//    end;
//  end;
//end;

procedure TfrmPitanje3.FormActivate(Sender: TObject);
begin
  Left := Round((Screen.Width - Width) / 2);
  Top := Round((Screen.Height - Height) / 2);
end;

procedure TfrmPitanje3.FormClose(Sender: TObject; var Action: TCloseAction);
begin
  Application.Terminate;
end;

procedure TfrmPitanje3.NazadClick(Sender: TObject);
begin
  if frmPitanje2.Pitanje2ID = 1 then
  begin
    frmPitanje2Knjiga.Show;
    self.Hide;
  end
  else
  begin
    frmPitanje2Tema.Show;
    self.Hide;
  end;
end;

function TfrmPitanje3.ProveraNizaStraneManje(const brojStrana: Integer; const OriginalniNiz: TArray<Integer>): TArray<Integer>;
var
  MyQuery: TFDQuery;
  ResultArray: TArray<Integer>;
  i: Integer;
  Found: Boolean;
  KnjigaID: integer;
begin
  SetLength(ResultArray, 0);

  MyQuery := TFDQuery.Create(nil);
  try
    MyQuery.Connection := GlobalConnection;
    MyQuery.SQL.Text := 'SELECT IDKnjige FROM Knjige WHERE Broj_Strana <= :Strane';
    MyQuery.ParamByName('Strane').AsInteger := brojStrana;

    // Set FetchOptions to ensure all records are fetched
    MyQuery.FetchOptions.Mode := fmAll;
    MyQuery.FetchOptions.RecsMax := -1; // Make sure there's no limit

    MyQuery.Open;

    ShowMessage('Broj elemenata u OriginalniNiz: ' + Length(OriginalniNiz).ToString);
    ShowMessage('Broj Clanova kverija: ' + (MyQuery.RecordCount).ToString);

    SetLength(ResultArray, MyQuery.RecordCount);
    while not MyQuery.Eof do
    begin
      KnjigaID := MyQuery.FieldByName('IDKnjige').AsInteger;

//      Found := False;
//      for i := Low(OriginalniNiz) to High(OriginalniNiz) do
//      begin
//        if KnjigaID = OriginalniNiz[i] then
//        begin
//          Found := True;
//          Break;
//        end;
//      end;
//
//      if Found then
//      begin
//        SetLength(ResultArray, Length(ResultArray) + 1);
//        ResultArray[Length(ResultArray) - 1] := KnjigaID;
//      end;

      MyQuery.Next;
    end;
  finally
    MyQuery.Free;
  end;

  Result := ResultArray;
end;

function TfrmPitanje3.ProveraNizaStraneIzmedju(const brojStranaManje: Integer; const brojStranaVece: Integer; const OriginalniNiz: TArray<Integer>): TArray<Integer>;
var
  MyQuery: TFDQuery;
  ResultArray: TArray<Integer>;
  i: Integer;
  Found: Boolean;
  KnjigaID: integer;
begin
  SetLength(ResultArray, 0);

  MyQuery := TFDQuery.Create(nil);
  try
    MyQuery.Connection := GlobalConnection;
    MyQuery.SQL.Text := 'SELECT IDKnjige FROM Knjige WHERE Broj_Strana >= :Strane_Manje AND Broj_Strana <= :Strane_Vece';
    MyQuery.ParamByName('Strane_Manje').AsInteger := brojStranaManje;
    MyQuery.ParamByName('Strane_Vece').AsInteger := brojStranaVece;

    // Set FetchOptions to ensure all records are fetched
    MyQuery.FetchOptions.Mode := fmAll;
    MyQuery.FetchOptions.RecsMax := -1; // Make sure there's no limit

    MyQuery.Open;

    ShowMessage('Broj elemenata u OriginalniNiz: ' + Length(OriginalniNiz).ToString);
    ShowMessage('Broj Clanova kverija: ' + (MyQuery.RecordCount).ToString);

    SetLength(ResultArray, MyQuery.RecordCount);
    while not MyQuery.Eof do
    begin
      KnjigaID := MyQuery.FieldByName('IDKnjige').AsInteger;

//      Found := False;
//      for i := Low(OriginalniNiz) to High(OriginalniNiz) do
//      begin
//        if KnjigaID = OriginalniNiz[i] then
//        begin
//          Found := True;
//          Break;
//        end;
//      end;
//
//      if Found then
//      begin
//        SetLength(ResultArray, Length(ResultArray) + 1);
//        ResultArray[Length(ResultArray) - 1] := KnjigaID;
//      end;

      MyQuery.Next;
    end;
  finally
    MyQuery.Free;
  end;

  Result := ResultArray;
end;

function TfrmPitanje3.ProveraNizaStraneVise(const brojStrana: Integer; const OriginalniNiz: TArray<Integer>): TArray<Integer>;
var
  MyQuery: TFDQuery;
  ResultArray: TArray<Integer>;
  i: Integer;
  Found: Boolean;
  KnjigaID: integer;
begin
  SetLength(ResultArray, 0);

  MyQuery := TFDQuery.Create(nil);
  try
    MyQuery.Connection := GlobalConnection;
    MyQuery.SQL.Text := 'SELECT IDKnjige FROM Knjige WHERE Broj_Strana >= :Strane';
    MyQuery.ParamByName('Strane').AsInteger := brojStrana;

    // Set FetchOptions to ensure all records are fetched
    MyQuery.FetchOptions.Mode := fmAll;
    MyQuery.FetchOptions.RecsMax := -1; // Make sure there's no limit

    MyQuery.Open;

    ShowMessage('Broj elemenata u OriginalniNiz: ' + Length(OriginalniNiz).ToString);
    ShowMessage('Broj Clanova kverija: ' + (MyQuery.RecordCount).ToString);

    while not MyQuery.Eof do
    begin
      KnjigaID := MyQuery.FieldByName('IDKnjige').AsInteger;

//      Found := False;
//      for i := Low(OriginalniNiz) to High(OriginalniNiz) do
//      begin
//        if KnjigaID = OriginalniNiz[i] then
//        begin
//          Found := True;
//          Break;
//        end;
//      end;
//
//      if Found then
//      begin
//        SetLength(ResultArray, Length(ResultArray) + 1);
//        ResultArray[Length(ResultArray) - 1] := KnjigaID;
//      end;

      MyQuery.Next;
    end;
  finally
    MyQuery.Free;
  end;

  Result := ResultArray;
end;

end.

