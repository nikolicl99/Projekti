unit Vise;

interface

uses
  System.SysUtils, System.Types, System.UITypes, System.Classes, System.Variants,
  FMX.Types, FMX.Controls, FMX.Forms, FMX.Graphics, FMX.Dialogs,
  FMX.Controls.Presentation, FMX.StdCtrls, System.Rtti, FMX.Grid.Style,
  FMX.Grid, FMX.ScrollBox, FMX.Edit, FireDAC.Stan.Intf, FireDAC.Stan.Option,
  FireDAC.Stan.Param, FireDAC.Stan.Error, FireDAC.DatS, FireDAC.Phys.Intf,
  FireDAC.DApt.Intf, FireDAC.Stan.Async, FireDAC.DApt, Data.DB,
  FireDAC.Comp.DataSet, FireDAC.Comp.Client;

type
  TFrmVise = class(TForm)
    Forma: TPanel;
    StyleBook: TStyleBook;
    StyleBook1: TStyleBook;
    Edit1: TEdit;
    Edit2: TEdit;
    Teme: TStringGrid;
    Teme_col: TStringColumn;
    procedure FormActivate(Sender: TObject);
  private
    { Private declarations }
    function GetPodzanrKnjige(const idKnjige: Integer): Integer;
    function GetPodzanrNaziv(const idPodzanra: Integer): String;
    function GetStraneKnjige(const idKnjige: Integer): String;
  public
    { Public declarations }
  end;

var
  FrmVise: TFrmVise;

implementation

{$R *.fmx}
uses Main;

procedure TFrmVise.FormActivate(Sender: TObject);
var
MyQuery: TFDQuery;
begin
Left := Round(((Screen.Width - Width) / 2) - 480);
Top := Round((Screen.Height - Height) / 2);
MyQuery := TFDQuery.Create(nil);
try
  MyQuery.Connection := GlobalConnection;
  MyQuery.SQL.Text := 'SELECT * FROM Tema_Knjige WHERE IDKnjige = :Knjiga';
  MyQuery.ParamByName('Knjiga').AsInteger := frmMain.CurrentBookID;
  MyQuery.Open;
finally

end;
end;

function TfrmVise.GetPodzanrKnjige(const idKnjige: Integer): Integer;
var
  MyQuery: TFDQuery;
begin
  Result := -1;
  MyQuery := TFDQuery.Create(nil);
  try
    MyQuery.Connection := GlobalConnection;
    MyQuery.SQL.Text := 'SELECT Podzanr FROM Knjige WHERE IDKnjige = :Knjiga';
    MyQuery.ParamByName('Knjiga').AsInteger := idKnjige;
    MyQuery.Open;

    if not MyQuery.IsEmpty then
      Result := MyQuery.FieldByName('Podzanr').AsInteger;
  finally
    MyQuery.Free;
  end;
end;

function TfrmVise.GetPodzanrNaziv(const idPodzanra: Integer): String;
var
  MyQuery: TFDQuery;
begin
  Result := '';
  MyQuery := TFDQuery.Create(nil);
  try
    MyQuery.Connection := GlobalConnection;
    MyQuery.SQL.Text := 'SELECT NazivPodzanra FROM Podzanr WHERE IDPodzanra = :Podzanr';
    MyQuery.ParamByName('Podzanr').AsInteger := idPodzanra;
    MyQuery.Open;

    if not MyQuery.IsEmpty then
      Result := MyQuery.FieldByName('NazivPodzanra').AsString;
  finally
    MyQuery.Free;
  end;
end;

function TfrmVise.GetStraneKnjige(const idKnjige: Integer): String;
var
  MyQuery: TFDQuery;
begin
  Result := '';
  MyQuery := TFDQuery.Create(nil);
  try
    MyQuery.Connection := GlobalConnection;
    MyQuery.SQL.Text := 'SELECT Naziv FROM Knjige WHERE IDKnjige = :Knjiga';
    MyQuery.ParamByName('Knjiga').AsInteger := idKnjige;
    MyQuery.Open;

    if not MyQuery.IsEmpty then
      Result := MyQuery.FieldByName('Naziv').AsString;
  finally
    MyQuery.Free;
  end;
end;

end.
